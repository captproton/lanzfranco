				<div class="page-header">
					<h1>Books</h1>
				</div>
				<div class="col-md-10">
					<div class="row">
						<table class="table table-striped">
							<thead>
								<tr>
									<th>Book Title</th>
								</tr>
							</thead>
							<tbody>
							    <% @books.each do |book| %>
							      <tr>
							        <td><%= link_to book.title, book %></td>
							        <td>
<%= link_to 'Edit', edit_book_path(book), class: "btn btn-default btn-xs"%>
<%= link_to 'Delete', book, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-xs btn-danger" %></td>
							      </tr>
							    <% end %>
								
							</tbody>
						</table>
						<%= link_to 'New', new_book_path, class: "btn btn-primary" %>
					</div>
				</div>
